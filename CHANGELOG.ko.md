# 변경 이력

Pedicle Screw Simulator의 주요 변경 사항을 기록합니다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르며,
버전 번호는 [유의적 버전(Semantic Versioning)](https://semver.org/lang/ko/)을
따릅니다. 저장소 최상위의 `VERSION` 파일이 현재 버전의 유일한 기준이며,
버전이 표시되는 다른 모든 위치는 이 파일에서 파생됩니다.

> **연구 및 교육 목적으로만 사용하십시오.** 이 문서의 어떤 내용도 임상적
> 검증을 뜻하지 않습니다. 자동 분할, 스크류 제안, 길이·직경, breach grade와
> 경고는 반드시 자격을 갖춘 의료진이 독립적으로 확인해야 합니다.

## [Unreleased]

작업 단계 표시줄을 따라가는 단계별 오른쪽 패널, 화면 전체가 아니라 선택한
Screw MPR 스크류의 등급 판정 기준이 되는 척추 하나만 여는 3D 절단, 그리고
압박골절이나 Schmorl node처럼 신뢰할 수 없는 상위 종판 적합을 그대로 따라
스크류를 기울이는 대신 가장 가까운 적합이 양호한(well-fitted) 이웃 레벨을
빌려 쓰도록 한 플래너 수정이 포함됩니다.

### 추가

- 세 탭 위젯 위에 고정되어 있던 "Planning Cockpit" 요약을 대신하는 4페이지
  단계 패널(Study / Segment / Plan / Review). Review 페이지는 레벨별 스크류
  목록을 중심으로 구성되며, 목록 위에는 선택한 스크류의 핵심 수치(레벨/side,
  직경, 길이, 등급)가 크게 표시되고, 동작 버튼은 목록 바로 아래에, 경고는
  "⚠ N warnings" 한 줄로 접혀 있으며, 나머지 지표는 Details 섹션에 접혀
  있습니다.
- 스크류 계획 표의 마지막 열이 이제 행마다의 경고 개수(⚠)이며, 기존 Source
  열을 대체합니다(자세한 내용은 "변경" 참고).
- 3D 화면 머리글에 **Vertebrae / Full CT** 토글이 추가되어, 3D에서 CT
  volume이 보이는지 여부를 항상 반영하며 이제 그 여부를 직접 결정합니다.
- TotalSegmentator 실행이 척추 라벨을 감지하면(threshold fallback은
  제외) 척추를 자동으로 isolate하고, 패널을 Plan 단계로 전환합니다.
- Screw MPR의 3D cross-section이 이제 실제 텍스처가 입혀진 CT 단면이 되어,
  Cross-section 창과 똑같이 Position·회전·plane 오프셋을 따라가며 Study 페이지의
  Window/Level 설정으로 렌더링됩니다. 선택한 스크류의 등급 판정 기준이 되는 척추만
  그 평면에서 열려 불투명하게 표시되고 주변은 흐리게 처리되며, 다른 모든
  레벨은 Vertebrae·Full CT 두 모드 모두에서 그대로 남습니다. 새로 추가된
  **Cut View** 버튼은 entry 쪽에서 스크류를 따라 cross-section을 내려다보도록
  카메라를 맞춰 줍니다.
- `endplate_reference`(`own` / `neighbours` / `none`)가 스크류의 궤적과
  각도가 실제로 어느 상위 종판 적합을 기준으로 측정되었는지 기록합니다.
  Review 페이지 Details 섹션에 표시되고 CSV의 마지막 열로 내보내집니다.

### 변경

- 작업 단계 표시줄은 이제 4페이지 사이를 이동하는 역할만 하며, 더 이상 Open
  DICOM, Run Auto Segmentation, Plan Screws를 직접 실행하지 않습니다.
- 척추 레벨 체크박스("Visible / Plan Levels")가 Study 탭의 Segmentation
  섹션에서 Plan 페이지("Levels to plan")로 옮겨졌으며, Full CT 모드에서 CT
  volume 자체를 가리거나 보여주는 역할은 더 이상 하지 않습니다 — 이제 3D
  헤더 토글의 역할입니다.
- Tools 탭의 "Validation" 섹션이 Study 페이지로 옮겨지면서 이름도
  "3D Rendering"으로 바뀌었습니다.
- Isolate Vertebrae 버튼과 3D **Vertebrae / Full CT** 토글은 이제
  threshold-fallback mask(척추 라벨이 없음)에서는 비활성화됩니다. 이전에는
  fallback을 포함해 segmentation이 끝나기만 하면 Isolate Vertebrae가
  활성화되었습니다.
- Source 열이 스크류 계획 표에서 접이식 Details 섹션으로 옮겨졌습니다. CSV
  내보내기에는 원래대로 그대로 남아 있습니다.
- Measurements가 Tools 탭에서 Review 페이지 하단의 접이식 섹션으로
  옮겨졌습니다. 이 섹션은 스크류 목록, 동작 버튼, Screw MPR 컨트롤,
  Details 다음에 위치합니다.

### 수정

- Screw MPR이 더 이상 3D 모델을 사라지게 하지 않습니다. 이전에는 volume
  전체와 척추 메시 전체를 하나의 무한 평면으로 잘랐는데, oblique한 스크류의
  경우 절단면보다 cranial한 모든 것이 사라졌고, 모든 레벨에서 해당 스크류의
  척추뿐 아니라 모든 척추의 후방 요소가 함께 잘려나갔습니다.
- 자체 상위 종판 적합을 신뢰할 수 없는 레벨(RMSE 3.0 mm 초과, 또는 적합
  자체가 없음 — L1/L2에서 나타난 것과 같은 압박골절이나 Schmorl node)이 더
  이상 그 망가진 적합을 따라 스크류를 기울이지 않습니다. 대신 위아래 두
  레벨 이내의 가장 가까운 적합이 양호한 레벨들의 역거리 가중 평균을 따라
  정렬하며(S1과 sacrum은 이웃 레벨 기준 차용에서 빌려주지도 빌리지도
  않음), 그런 이웃이 없으면 수평 궤적으로 대체합니다. 플래너와 ScrewTool의
  재등급 모두 실제로 사용된 기준에 대해 종판각을 보고합니다.
- 척추경 subregion 옵션을 UI에서 열 방법이 없었습니다(0.2.1부터 이미
  그랬음). 이제 Segment 단계의 **Advanced options** 토글로 열 수
  있습니다.

## [0.2.1] - 2026-09-12

세 가지 주요 계획 단계를 안내하는 작업 단계 표시줄(workflow bar), 3D에서 보는
Screw MPR, 그리고 나사 head를 후방 피질골(dorsal cortex)에 배치하고 각 나사를
anterior margin까지 늘리며, 등급은 head가 아니라 축이 뼈에 들어간 지점부터
매기는 플래너 수정이 포함됩니다.

### 추가

- MPR/3D 화면 위에 "① Open DICOM → ② Segment → ③ Plan Screws"를 표시하는
  작업 단계 표시줄. 각 단계는 이름이 가리키는 기존 동작을 그대로 실행합니다.
  완료된 단계에는 체크 표시가 붙고, 다음 단계는 비활성 상태라도 강조되며,
  비활성 단계의 tooltip은 무엇이 있어야 풀리는지 알려줍니다. ② 단계는 실제
  TotalSegmentator mask가 있어야 완료로 인정됩니다(threshold fallback은
  인정하지 않음). ③ 단계는 자동으로 계획된 나사가 있어야 완료로 인정되며,
  이를 지우면 다시 열립니다. 새 study를 불러오면 표시줄이 초기화됩니다.
- Screw MPR에 이제 3D 대응 화면이 있습니다. 3D 화면은 표준
  axial/sagittal/coronal 표시 대신, 나사 축에 정렬된 세 plane을 보여줍니다.
  각 plane은 이를 보여주는 창의 머리글과 같은 색의 80 mm 정사각형입니다.
  CT volume과 척추 메시는 cross-section에서 잘려 tip 쪽만 남으며, 절단면은
  Position을 따라 이동합니다. 나사 자체는 절대 잘리지 않습니다.

### 변경

- **나사 head가 이제 나사 자체 축을 따라 후방 피질골(dorsal cortex)에
  배치됩니다** — 중심선이 마지막으로 만나는 뼈, 즉 실제로 드릴이 시작될
  위치입니다 — 이전에는 단면 전체가 처음 들어맞는 위치에 배치되었습니다.
  그 축을 따라 뒤쪽 15 mm 이내에 같은 척추의 뼈가 여전히 있으면 도달
  불가능한 것으로 제외합니다. 이는 예전의 6 mm posterior cortex 매몰 한계를
  대체합니다.
- **등급 산정은 이제 head가 아니라, 나사 축이 뼈에 들어가는 지점에서 3 mm
  지난 곳부터 시작합니다**(`ENTRY_ZONE_MM`). 피질골에 배치된 head는 뼈에
  들어가는 표면에 걸쳐 있어 자기 반경만큼의 breach로 읽혔기 때문입니다.
  이는 등급이 매겨지는 모든 나사에 적용됩니다: Optimizer와 Legacy의 자동
  제안(CBT 나사에 표시되는 등급 포함), 수동·수정된 나사, segmentation
  또는 계획 불러오기 후 다시 매기는 등급.
- **나사 길이는 이제 팁 앞쪽에 Anterior margin(기본 4 mm)만큼의 뼈를 남기는
  가장 긴 카탈로그 길이이며, 팁에서 anterior 척추체 피질골까지 나사
  자체 축을 따라 측정합니다.** 원위 원통 전체를 둘러싼 여유거리를 요구하던
  이전 방식 대신, 원위부 shaft는 이제 나머지 shaft와 마찬가지로 포함 조건과
  설정된 Wall clearance만 만족하면 됩니다. 각 궤적에서는 실현 가능한 가장
  긴 길이만 순위 산정에 포함되므로, Length 가중치는 한 궤적 위의 길이가
  아니라 궤적들 사이를 비교합니다.
- Legacy 플래너와 자동 per-side legacy fallback은 먼저 궤적을 검증한 뒤,
  다음 순서로 시도합니다: head를 후방 피질골에 재배치하고(같은 15 mm
  후방 접근로 검사를 통과할 때만) tip을 Anterior margin까지 늘린 나사,
  원래 head에서 tip만 같은 방식으로 늘린 나사, 검증된 나사 그대로. 이
  중 검증된 나사보다 천공이 늘지 않는(medial 천공을 먼저 비교하고,
  같으면 전체 천공을 비교) 첫 번째 나사를 사용하므로, 길이를 늘리거나
  head를 옮기려고 legacy 나사의 안전성을 낮추는 일은 없습니다.
  Cortical bone trajectory(CBT) 선택 방식은 변경되지 않았습니다.
- `Planes On / Off`는 이제 표준이든 나사 정렬이든 현재 표시 중인 plane
  세트에 그대로 적용됩니다.
- 불러올 때나 segmentation을 다시 실행한 뒤 다시 등급을 매긴 계획은 진입부
  피질골이 더 이상 채점되지 않으므로, 예전 빌드가 저장했을 때보다 더 좋은
  등급으로 나올 수 있습니다 — 계획 파일 자체는 바뀌지 않습니다.

### 수정

- 0.2.0 README와 사용설명서에 테마가 네 가지("어두운 테마 하나와 밝은 테마 셋")라고
  잘못 적혀 있었습니다. 실제 테마는 0.1.0부터 그대로 세 가지(Soft Light, Graphite
  Blue, Graphite Mint)이며, 0.2.0에서는 기본 테마만 Graphite Blue로 바뀌었습니다.
  아래 0.2.0 항목도 이에 맞게 정정했습니다.
- 계획된 나사가 짧고 head가 뼈에 묻혀 있었습니다. 샘플 연구에서 각 나사
  자체 축을 따라 측정했을 때 head는 lamina 안쪽 5–21 mm에 있었고 tip은
  anterior cortex보다 5–20 mm 못 미치는 곳에서 멈췄습니다. 샘플 연구(정제된
  mask, 기본 설정)에서:

  |            | 이전               | 이후                    |
  |------------|--------------------|--------------------------|
  | 나사 수    | 12개               | 13개(S1-left 새로 계획)  |
  | legacy 대체 | 1건               | 0건                      |
  | 등급       | A 10 / B 2         | A 13                     |
  | medial breach | L5-right 1.41 mm | 모든 곳에서 0           |
  | head 매몰  | 5–21 mm            | 0–0.2 mm                 |

  길이(좌/우)는 L1 35/35, L2 50/45, L3 35/40, L4 40/35, L5 45/35,
  T12 35/25 mm가 되었으며, 이전에는 대부분의 side가 25 mm로 제한되어
  있었습니다(좌측 L1, L2, L4 포함).
- Screw MPR이 활성화된 동안 3D 화면이 volume을 자르지 않은 채 표준
  axial/sagittal/coronal plane을 계속 그리던 문제.

## [0.2.0] - 2026-09-11

공개 baseline 이후 첫 번째 릴리스입니다. 자동 궤적 최적화, 좁은 척추경에 대한
임상 정책, 피질골 궤적(CBT), CT 기반 마스크 정제, 그리고 대폭 개편된 인터페이스가
포함됩니다.

### 추가

**계획(Planning)**

- 다목적 궤적 최적화기가 **기본 계획 모드**가 되었습니다. 수렴각, 두미각,
  진입 위치, 길이에 걸쳐 후보 궤적을 생성한 뒤 안전성, 골밀도, 길이, 종판
  일치도, 척추경 중심성으로 점수를 매깁니다. 기존 규칙 기반 플래너는
  **Legacy** 모드로 남아 있으며, 한쪽 측면이 풀리지 않을 때 자동 대체로도
  쓰입니다.
- Construct 단위 조정: 같은 쪽 진입점을 로드 직선에 맞추고 인접 레벨의
  수렴각을 서로 가깝게 모읍니다. 이때 어떤 나사도 자신의 최고 안전 점수의
  90% 아래로 내려가지 않습니다. 상태 표시줄의
  `Construct: rod fit … · convergence spread …`와 검사창의 **Alignment** 행에
  표시됩니다.
- **좁은 척추경 정책.** 척추경이 좁다는 이유로 측면을 건너뛰지 않습니다.
  설정된 임계값(기본 5.0 mm) 미만이면 카탈로그에서 가장 작은 임플란트를
  사용하고 빨간색으로 표시하며, **medial(척추관 쪽) 벽을 보호**하도록
  계획합니다 — medial 천공 0, lateral 천공 상한 적용, 두 조건이 성립할 때까지
  진입점을 외측으로 이동. in-out-in 술기를 집도의가 알아서 판단하도록 두지
  않고 명시적으로 구현한 것입니다.
- **상부 종판 평행(Parallel to upper endplate)** 계획 옵션(기본 켜짐)과
  설정 가능한 허용 범위, 그리고 나사별 부호 있는 종판 각도 표시.
- **피질골 궤적(CBT)** 계획 모드. 합의된 기본 직경·길이, 협부 하내측 모서리를
  기준으로 한 고유 진입 랜드마크, 모든 CBT 나사에 붙는 금기 안내를 포함합니다.
- UI에서 직접 편집 가능한 계획 파라미터: 척추경 충전비, wall clearance,
  전방 여유, 최대 수렴각, 궤적 HU 임계값, 좁은 척추경 임계값, 좁은 측면의
  lateral 천공 상한, 종판 허용 범위, 플래너 모드, 궤적 종류, 최적화기 가중치 3종.
- 잘라낸 유클리드 거리 지도 기반 Gertzbein–Robbins 등급 산정. medial,
  lateral, 두미 방향으로 천공을 나누어 측정하고 남은 medial 벽 두께도 보고합니다.
- 모든 나사에 붙는 골질·천공 지표: 문헌 임계값을 곁들인 궤적·척추경·척추체 HU,
  궤적 대 척추체 HU 비율, Heary 천공 방향, facet 침범 등급.
- 두 계획을 비교하는 `scripts/validate_plans.py`와 비교 지표(평균 절대 편차,
  축 각도, 원통 Dice, Bland–Altman).
- `scripts/check_narrow_policy.py` — 실제 연구 데이터에 대해 분석기와 플래너를
  돌려 좁은 척추경 정책 위반 시 실패하는 Qt 비의존 검수용 CLI.

**분할 및 측정**

- TotalSegmentator 이후의 **CT 기반 마스크 정제**: 라벨별 안티에일리어싱,
  CT를 기준으로 재판정하는 경계 밴드, 3D 구멍 메우기, 최대 연결성분 선택.
  1.5 mm 추론 마스크를 1 mm 미만 CT 격자로 업샘플링할 때 생기는 계단 현상을
  제거합니다. 취소 가능하며, 부피가 과하게 변한 정제는 거부하는 안전장치가
  있습니다.
- 로컬에 설치한 척추경 세부영역 nnU-Net 단계를 선택적으로 실행해 라벨 기반
  협부 경계를 분석기에 전달합니다(소스 설치 전용).
- 견고해진 척추경 폭 측정: 슬라이스 간 중심 연속성 추적, 얇은 조각에 대한
  면적·폭 하한, 내접원 추정치와 교차 확인하는 이웃 중앙값 폭, 레벨별 타당
  범위 검사와 축상 재측정, 척추경 축 기울기 가드.
- 상부 종판 평면 적합과 RMSE 보고, 적합이 신뢰하기 어려울 때의 경고.
- TotalSegmentator 하위 프로세스를 실제로 종료하는 취소 버튼.

**인터페이스**

- Axial 화면이 방사선과 관례대로 **anterior가 위쪽**으로 열리며, 모든 MPR
  창에 **A / P / L / R** 방향 표시가 붙습니다.
- **Screw MPR 창이 이제 움직입니다.** 나사 축을 중심으로 oblique 평면을
  회전하고, 나사를 따라 또는 가로질러 평행이동하며, 나사 정렬 검토를 벗어나지
  않고 스크롤·드래그할 수 있습니다. 회전과 오프셋은 창 판독값에 표시됩니다.
- **Screw Review 표**: 나사별 레벨, 측면, 척추경 폭, 직경, 길이, 등급 칩,
  출처를 한눈에 보여주며 기존 단순 목록을 대체합니다.
- 제어 탭 위의 계획 조종석: 선택한 나사의 직경, 길이, 수렴각, 두미각, 종판각,
  construct 정렬, 등급, 궤적 HU, 척추체 HU, 벽 여유, facet, Heary 방향,
  궤적 종류, 척추경 판정을 표시합니다.
- 탭으로 나뉜 제어 패널(Study / Planning / Tools)과 도구 아이콘.
- 헤더 더블클릭 또는 Ctrl+M으로 창 최대화. 포인터가 작업 중인 창을 따라갑니다.
- 나사 메타데이터, 부호 있는 각도, 전체 지표 묶음을 담는 JSON 계획 스키마 v3.
  CSV 내보내기에 척추경 폭, narrow/width-uncertain 플래그, 방향별 천공,
  종판 각도가 추가되었습니다.

**프로젝트**

- macOS·Windows standalone 데스크톱 패키징(TotalSegmentator 번들 포함).
- Windows PowerShell 실행기(`scripts/run_app.ps1`).
- 모든 push와 pull request에서 ruff와 pytest를 실행하는 CI.
- MIT 라이선스, 학술 출처 표기, `CITATION.cff`, 서드파티 고지.
- 이 변경 이력 문서.

### 변경

- **기본 플래너 모드가 Optimizer로 바뀌었습니다.** 이전 빌드에 저장된
  `legacy` 모드는 최초 1회 이관되며 상태 표시줄에 안내가 나옵니다. 이관 이후
  사용자가 직접 고른 Legacy 모드는 그대로 존중됩니다.
- **기본 wall clearance가 0 mm가 되었습니다.** 1 mm에서는 실제 연구 데이터의
  대부분 측면에서 최적화기가 포함 조건을 만족하지 못해 조용히 legacy 플래너로
  넘어갔습니다. 예전 기본값이던 1.0 mm(사용자가 고른 값이 아님)는 최초 1회
  0으로 재설정되며 상태 표시줄에 안내가 나옵니다. 그 밖의 저장값은 건드리지
  않습니다.
- 분석기가 신뢰할 수 없다고 판단한 폭은 "narrow"가 아니라 **"신뢰할 수 없음"**
  으로 표시됩니다(계획 표, 조종석, 경고, CSV 내보내기 모두). 좁은 척추경 정책은
  동일하게 적용되며 표현만 바뀌었습니다 — 타당 범위 검사는 비정상적으로 *넓은*
  측정값도 걸러내기 때문입니다.
- macOS를 제외한 모든 플랫폼에서 GPU 가능 볼륨 렌더링(`vtkSmartVolumeMapper`)을
  사용합니다. macOS는 OpenGL→Metal 변환 계층이 3D 텍스처 업로드에서 멈추므로
  CPU 레이캐스터를 유지합니다. GPU를 찾지 못해 CPU로 떨어진 경우에는 전해상도로
  갈아내는 대신 다시 다운샘플링합니다.
- 나사 직경이 비는 일이 없습니다. 카탈로그 최소 임플란트보다도 작은 척추경도
  측면을 비워두는 대신 최소 직경 나사를 받고 표시됩니다.
- 기본 테마가 밝은 Soft Light에서 어두운 Graphite Blue로 바뀌었습니다. 제공되는
  테마는 그대로 세 가지이며, 이미 선택해 둔 테마는 유지됩니다.
- 계획 파일에 환자 식별정보가 담기지 않음을 검증합니다.
- ruff import 정렬과 린트 규칙을 저장소 전체에 적용하고, 파일 단위 예외를
  제거했습니다.

### 수정

- **일부 레벨만 계획되던 문제.** 관상면 협부 탐색이 정중선에 가장 가까운
  성분을 택하는 바람에 1.6 mm 계단 조각이 실제 7.4 mm 척추경을 밀어내고, 그
  레벨이 "너무 좁음"으로 버려졌습니다. 샘플 연구에서 측정된 L3 폭이
  3.1 / 4.7 mm에서 8.6 / 8.8 mm로 바로잡혔습니다.
- **최적화기가 대부분의 측면에서 legacy로 대체되던 문제.** 후보 생성은 수렴각을
  척추경 축 기준 상대값으로 훑는데 점수 계산은 절대 각도로 걸러내어, 축이 크게
  수렴한 레벨은 모든 후보가 기각되었습니다. 샘플 연구에서 등급이
  A:2 / B:10(legacy 대체 10건)에서 A:10 / B:2(대체 1건)로 개선되었습니다.
- 분석기가 찾지 못한 척추경이 0.0 mm 측정값으로 읽혀, 수동 배치한 나사가
  빨갛게 칠해지고 "4.0 mm 나사가 척추경의 100%를 채운다"는 경고가 나가던 문제.
- 같은 연구를 재분할하면 새 마스크로 등급을 다시 매기면서 척추경 폭, 좁음 여부,
  종판 각도는 옛 마스크 기준 분석에서 다시 가져오던 문제.
- 축상 폭 재측정이 거리·측면 확인 없이 가장 가까운 비척추체 성분을 받아들여,
  횡돌기 조각이 실제 협부 측정값을 조용히 대체할 수 있던 문제.
- Construct 정렬 지표가 계획 시점에 한 번만 측정되고 갱신되지 않아, 검사창의
  Alignment 행이 사용자가 이미 옮긴 궤적을 계속 설명하던 문제.
- CBT 나사가 상부 종판 법선을 전달받지 못해 종판 각도가 첫 드래그 이후에야
  나타나던 문제.
- 저장소의 다른 플래너 키 하나가 읽히지 않으면, 1회성 설정 이관이 정작 없애야
  할 값을 남겨둔 채 "완료됨"으로 기록되던 문제.
- Ctrl+M이 작업 중인 창이 아니라 마지막으로 최대화했던 창을 최대화하던 문제.
- HiDPI 화면에서 방향 표시와 Screw MPR 회전 중심이 장치 픽셀이 아닌 Qt 논리
  픽셀을 쓰던 문제.
- 최대화에서 복원한 3D 창이 다음 조작 전까지 비어 있을 수 있던 문제.
- Oblique 볼륨 resampling의 축 배정이 항상 올바른 순열이 아니던 문제.
- 그 밖의 등급·드래그·계획 수명주기 관련 다수 수정: 마스크 밖을 지나는 드래그
  중에 등급과 지표가 깜빡이거나 남지 않고, 교체된 계획 실행이 중지되며, 설명
  대상 연구가 바뀌면 레벨별 분석이 폐기됩니다.

### 이관 안내

- 최초 실행 시 1회성 설정 이관 두 가지가 실행되고 상태 표시줄에 안내가 나옵니다:
  플래너 모드 `legacy` → `optimizer`, wall clearance `1.0 mm` → `0 mm`. 두 이관
  모두 기록되어 다시 실행되지 않으며, 이후 사용자가 선택한 값은 존중됩니다.
- 0.1.0에서 저장한 계획은 그대로 열립니다. 0.2.0이 저장하는 계획은 스키마 v3를
  사용하며, 이전 버전이 무시하는 지표를 함께 담습니다.
- CSV 내보내기에 열이 추가되었습니다. 항상 끝에 덧붙이고 기존 열의 순서를 바꾸지
  않으므로, 헤더 이름으로 읽는 프로그램은 그대로 동작합니다.

## [0.1.0] - 2026-07-12

최초 공개 baseline.

- LPS 재정렬과 oblique resampling을 포함한 다중 시리즈 DICOM CT 불러오기.
- Axial, Sagittal, Coronal MPR 동기화 및 이동·확대·window/level 조정.
- VTK 볼륨 렌더링과 척추별 3D 메시.
- GPU 우선 TotalSegmentator 연동 및 CPU 재시도(선택).
- 여러 척추 레벨 선택과 규칙 기반 자동 스크류 제안.
- 기본 MPR 및 스크류 정렬 oblique MPR 검토.
- MPR과 3D에서 entry, tip, 전체 스크류 직접 수정.
- 수동 스크류 추가, 거리 측정, 각도 측정.
- JSON 계획 저장·불러오기, CSV·STL 내보내기.
- 세 가지 UI 테마.

[0.2.1]: https://github.com/grotyx/pedicle-screw-simulator/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/grotyx/pedicle-screw-simulator/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/grotyx/pedicle-screw-simulator/releases/tag/v0.1.0
